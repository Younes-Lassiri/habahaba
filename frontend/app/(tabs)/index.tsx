import HomeScreenContent from "@/components/HomeScreenContent";

export default function HomeTab() {
  // You can optionally read the user language from AsyncStorage or context
  // and pass it as a prop. The component defaults to 'english'.
  return <HomeScreenContent />;
}